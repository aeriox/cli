#!/usr/bin/env node

// Reservation stub for @aeriox-co/cli@0.0.0.
// Replaced by the real oclif entrypoint at v1.0.0.
// Real entrypoint will be: `import { execute } from '@oclif/core'; await execute({ dir: import.meta.url });`

console.error(
  'This is a placeholder — install the real CLI from npm @aeriox-co/cli@^1.0.0 once it ships.'
);
process.exit(1);
